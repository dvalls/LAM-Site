%h1 Listing carousels

%table
  %tr
    %th Project
    %th Title
    %th Description
    %th Image url
    %th
    %th
    %th

  - @carousels.each do |carousel|
    %tr
      %td= carousel.project_id
      %td= carousel.title
      %td= carousel.description
      %td= carousel.image_url
      %td= link_to 'Show', carousel
      %td= link_to 'Edit', edit_carousel_path(carousel)
      %td= link_to 'Destroy', carousel, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Carousel', new_carousel_path

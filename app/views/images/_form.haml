= simple_form_for([@imageable, @image]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'

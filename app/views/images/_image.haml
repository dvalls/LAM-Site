%li
  .row
    .col-lg-3
      = image_tag image.url.url(:thumb)
    .col-lg-9
      %h3= image.title
      %p= text_field_tag 'description', image.description
      %p= text_field_tag 'images', image.url.url

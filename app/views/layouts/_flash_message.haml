- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{class: "pull-right alert col-lg-8 col-md-8 col-sm-8 hidden-xs alert-#{name == :notice ? "info" : "error"} alert-dismissible", role: "alert"}
      %a.close{"data-dismiss" => "alert"} ×
      = content_tag :div, msg, :id => "flash_#{name}"
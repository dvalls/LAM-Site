= simple_form_for(@image) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description

  .form-actions
    = f.button :submit, 'ATUALIZAR INFORMAÇÕES', :class => 'btn btn-primary'

%h1 Listing admin_tags

%ul
  - @tags.each do |tag|
    %li
      = tag.name
      = link_to 'editar', edit_admin_tag_path(tag)
      = link_to 'apagar', admin_tag_path(tag),  :method => :delete, :data => { :confirm => 'Are you sure?' }
%br

= link_to 'New Tag', new_admin_tag_path
